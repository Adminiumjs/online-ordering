node_modules
dist
dist-surface
*.tsbuildinfo
.DS_Store
*.local
.vercel

# .env holds a real ADMINIUM_SECRET — README and .env.example both tell the
# reader to put one there, so it must never be committable by a stray `git add`.
.env
.env.*
!.env.example
