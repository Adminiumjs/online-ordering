spec:
  name: online-ordering
  static_sites:
    - name: web
      github:
        repo: Adminiumjs/online-ordering
        branch: main
        deploy_on_push: true
      build_command: npm run build
      output_dir: dist
      environment_slug: node-js
