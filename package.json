{
  "name": "online-ordering",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Online Ordering — an open-source order-ahead example app for Adminium.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:demo": "tsc -b && vite build --base=/demo/online-ordering/",
    "build:surface:staff": "VITE_ADMINIUM_SURFACE_SIDE=staff tsc -b && VITE_ADMINIUM_SURFACE_SIDE=staff vite build --base=/apps/ordering/staff/ --outDir dist-surface/ordering/staff",
    "build:surface:customer": "VITE_ADMINIUM_SURFACE_SIDE=customer tsc -b && VITE_ADMINIUM_SURFACE_SIDE=customer vite build --base=/apps/ordering/customer/ --outDir dist-surface/ordering/customer",
    "build:surface": "npm run build:surface:staff && npm run build:surface:customer",
    "preview": "vite preview",
    "test": "vitest run",
    "demo:status": "node db/demo.mjs status",
    "demo:import": "node db/demo.mjs import",
    "demo:wipe": "node db/demo.mjs wipe",
    "demo:reset": "node db/demo.mjs reset",
    "dev:hosted": "VITE_ADMINIUM_SURFACE_SIDE=staff vite"
  },
  "dependencies": {
    "@adminiumjs/public-client": "^0.2.2",
    "lucide-react": "^0.462.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^7.0.0",
    "vitest": "^2.1.9",
    "zod": "^4.4.3"
  }
}
